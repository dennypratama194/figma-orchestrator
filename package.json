{
  "name": "figma-orchestrator",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "A gated brief-to-Figma design orchestrator for Claude Code.",
  "scripts": {
    "test": "node --test",
    "validate": "node scripts/validate-plugin.mjs",
    "check": "npm run validate && node scripts/validate-tokens.mjs templates/design-tokens.json && npm test",
    "orchestrator": "node scripts/orchestrator.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "yaml": "^2.8.1"
  }
}
